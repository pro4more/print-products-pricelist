{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
    <div class="container-fluid">
        <ul>
            <li>
                <a href="{{ path('product_index') }}">Product index</a>
            </li>
            <li>
                <a href="{{ path('available_format_index') }}">AvailableFormat index</a>
            </li>
            <li>
                <a href="{{ path('material_index') }}">Material index</a>
            </li>
            <li>
                <a href="{{ path('pricelist_entry_index') }}">PricelistEntry index</a>
            </li>
        </ul>
    </div>
{% endblock %}
