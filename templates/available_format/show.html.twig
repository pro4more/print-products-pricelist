{% extends 'base.html.twig' %}

{% block title %}AvailableFormat{% endblock %}

{% block body %}
    <h1>AvailableFormat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ available_format.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ available_format.name }}</td>
            </tr>
            <tr>
                <th>Width</th>
                <td>{{ available_format.width }}</td>
            </tr>
            <tr>
                <th>Height</th>
                <td>{{ available_format.height }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('available_format_index') }}">back to list</a>

    <a href="{{ path('available_format_edit', {'id': available_format.id}) }}">edit</a>

    {{ include('available_format/_delete_form.html.twig') }}
{% endblock %}
